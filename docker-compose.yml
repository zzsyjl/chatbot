services:
  chatbot:
    build: .
    container_name: chatbot
    restart: always
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: "production"
      PORT: "3000"
